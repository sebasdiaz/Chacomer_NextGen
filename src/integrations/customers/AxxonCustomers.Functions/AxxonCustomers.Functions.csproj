<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <AzureFunctionsVersion>v4</AzureFunctionsVersion>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>AxxonCustomers.Functions</RootNamespace>
    <AssemblyName>AxxonCustomers.Functions</AssemblyName>
    <Company>Axxon Consulting</Company>
    <Version>1.0.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <!-- Azure Functions runtime (.NET isolated process) -->
    <PackageReference Include="Microsoft.Azure.Functions.Worker" Version="2.52.0" />
    <!-- Solo por el backfill de LTMCustTable, que es un HTTP trigger. El resto de la app
         son triggers de Service Bus. Misma version que usa AxxonFiscal. -->
    <PackageReference Include="Microsoft.Azure.Functions.Worker.Extensions.Http" Version="3.3.0" />
    <PackageReference Include="Microsoft.Azure.Functions.Worker.Extensions.ServiceBus" Version="5.24.0" />
    <PackageReference Include="Microsoft.Azure.Functions.Worker.Sdk" Version="2.0.7" />
  </ItemGroup>

  <ItemGroup>
    <!-- Componentes cross de la EiP: Key Vault, Dataverse, F&O OData, retry, telemetria -->
    <ProjectReference Include="..\..\..\core\Axxon.Eip.Core\Axxon.Eip.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Mapeos CRM -> F&O: export de Dual Write (intacto) + overlay. Se compilan al
         arranque, asi que tienen que viajar con el deploy. -->
    <Content Include="Mappings\*.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <ItemGroup>
    <None Update="host.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="local.settings.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>Never</CopyToPublishDirectory>
    </None>
  </ItemGroup>

</Project>
