# Pipeline targeted de la integracion Customers (redeploy de una sola app).
# Manual: elegir environmentName. El release completo usa azure-pipelines-release.yml.

trigger: none
pr: none

parameters:
  - name: environmentName
    type: string
    default: inte
    values:
      - inte
      - uat
      - prod

extends:
  template: templates/functionapp-build-deploy.yml
  parameters:
    appKey: customers
    appName: 'fa-axxoncustomers-${{ parameters.environmentName }}'
    projectPath: 'src/integrations/customers/AxxonCustomers.Functions/AxxonCustomers.Functions.csproj'
    azureServiceConnection: 'sc-chacomer-eip-${{ parameters.environmentName }}'
    environmentName: ${{ parameters.environmentName }}
