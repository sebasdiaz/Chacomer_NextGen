# Pipeline targeted de la integracion Products (redeploy de una sola app).
# Manual: elegir environmentName. El release completo usa azure-pipelines-release.yml.

trigger: none
pr: none

parameters:
  - name: environmentName
    type: string
    default: inte
    values:
      - inte
      - uat
      - prod

extends:
  template: templates/functionapp-build-deploy.yml
  parameters:
    appKey: products
    appName: 'fa-axxonproducts-${{ parameters.environmentName }}'
    projectPath: 'src/integrations/products/AxxonProducts.Functions/AxxonProducts.Functions.csproj'
    azureServiceConnection: 'sc-chacomer-eip-${{ parameters.environmentName }}'
    environmentName: ${{ parameters.environmentName }}
