# Pipeline targeted de la integracion Contacts (redeploy de una sola app).
# Manual: elegir environmentName. El release completo usa azure-pipelines-release.yml.

trigger: none
pr: none

parameters:
  - name: environmentName
    type: string
    default: inte
    values:
      - inte
      - uat
      - prod

extends:
  template: templates/functionapp-build-deploy.yml
  parameters:
    appKey: contacts
    appName: 'fa-axxoncontacts-${{ parameters.environmentName }}'
    projectPath: 'src/integrations/contacts/AxxonContacts.Functions/AxxonContacts.Functions.csproj'
    azureServiceConnection: 'sc-chacomer-eip-${{ parameters.environmentName }}'
    environmentName: ${{ parameters.environmentName }}
